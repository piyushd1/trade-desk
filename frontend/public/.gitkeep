# This directory is intentionally tracked (even when empty) because
# deployment/docker/Dockerfile.frontend runs:
#     COPY --from=builder /app/public ./public
# which fails if the directory does not exist in the build context.
#
# Add static assets here: favicon.ico, robots.txt, images, etc.
# They will be served at the root URL path by Next.js.
